# Configuration STT (Speech-to-Text)
WHISPER_MODEL_SIZE=base
# Options: tiny, base, small, medium, large
# tiny: plus rapide, moins précis
# base: bon équilibre (recommandé)
# small, medium, large: plus précis mais plus lent

STT_LANGUAGE=pt
# Code langue ISO 639-1 (pt, fr, en, es, etc.)

STT_PREPROCESS=true
# Activer le pré-traitement audio (VAD, réduction de bruit, etc.)

# Configuration TTS (Text-to-Speech)
TTS_ENGINE=pyttsx3
# Options: pyttsx3 (offline) ou gtts (nécessite internet)

TTS_LANGUAGE=fr
# Code langue pour la synthèse vocale

# Port de l'API Python
PYTHON_API_PORT=8000
